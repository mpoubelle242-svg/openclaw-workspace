# Mémoire du 2026-03-17

## Résumé de la journée
Journée productive de développement sur Quantique-SaaS. Multiple sprints complétés, bugs majeurs corrigés.

## Sprints terminés
- **QS-10** — Refonte visuelle complète (typo, landing, dashboard, admin, audit)
- **QS-11** — Tests E2E Playwright installés et 19/19 passants
- **QS-12** — UX polish (icônes SVG, hover effects, empty states, spacing)
- **QS-13** (partiel) — Fix chunks JS 404, responsive CSS workaround

## Bugs majeurs corrigés
1. CORS — `NEXT_PUBLIC_API_URL` en dur dans `.env`
2. Route login `/auth/login` → `/login` (double prefix)
3. Redis auth — password `R3d1s_2026` manquant
4. Port backend — 3001 → 4001 (conflit avec Next.js)
5. JSX dupliqué — `</motion.p>` orphelines dans login/register
6. tailwind.config.ts supprimé accidentellement par agent → restauré
7. Chunks JS Next.js retournant 404 → restart serveur
8. Tailwind JIT ne générant pas les classes responsive → workaround manuel globals.css

## Leçons apprises
- Agents échouent quand trop de tâches → une seule tâche par agent
- Agent chef-de-projet produit des faux positifs → relancer avec instructions strictes
- Ne jamais supprimer tailwind.config.ts
- Spécifier PORT=4001 explicitement pour backend
- Toujours vérifier que les agents COMMITENT réellement
- MC : utiliser projet Quantique-SAAS (id=3), pas General (id=1)

## Contacts
- Yoann : +33648565222 (WhatsApp)

## Prochaine fois
- Terminer QS-13 (stabilisation serveur)
- Traduire login/register en français
- Harmoniser tokens couleur
- Tests Playwright avec scroll pour framer-motion
